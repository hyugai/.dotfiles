# determine which package manager the current operating system is using
# install Git -> config `user.email` and `user.name`
# install NeoVim -> make symbolic link for '~/.config/nvim'
# install Alacritty -> make symbolic link for ~/.config/alacritty
# install npm -> need this package to install bash-language-server(basls)
# install gcc -> need this package for C
# install gcc-c++ -> need this package for C++
# make hard link for `.clang-format` file
# make symbolic link for ~/.tmux.conf
# customize `PS1` string
